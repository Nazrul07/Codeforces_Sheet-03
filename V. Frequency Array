#include <iostream>
using namespace std;
int main() {
    int n,m,temp,count=0,j=1;
    cin>>n>>m;
    int a[n],arr[m+1]={};
    for(int i=0 ; i<n ; i++)
    {
        cin>>a[i];
        arr[a[i]]++;
    }
    for(int i=1 ; i<=m ; i++)
    {
        cout<<arr[i]<<endl;
    }
    return 0;
}
